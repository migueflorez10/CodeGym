// math object

let result; 

result = Math.PI;
result = Math.round(3.7);
result = Math.ceil(3.3); // always rounds up 
result = Math.floor(3.3); // always rounds down
result = Math.sqrt(144); // takes the square root
result = Math.abs(-144); // change a number from positive to negative
result = Math.min(2, 6, 4, 5, 9, 3); // minimum list number
result = Math.max(2, 6, 4, 5, 9, 3); // max list number
result = Math.random(); // random number
result = Math.floor(Math.random()*30); 

console.log(result);