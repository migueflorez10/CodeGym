// order of operations

let resut; 

resut = (20 + 30) * 2;

resut = (100 + 200 + 150) * .2; // promotion
resut = (100 + 200 + 150) * 1.16; //tax 

console.log(resut);

// increase

let score = 10; 
// increase in one 
score++; // add one point
score--; // delete points
score+=20; // add points

console.log(score);